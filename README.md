# Topology

A static website for Topology.

## Development

To run the project locally:

```bash
npm install
npm run dev
```

This will start a local server on port 8000.

## Build

The project is pre-built. To "build":

```bash
npm run build
```

## Deploy

The built files are in the `build/` directory and can be served directly.